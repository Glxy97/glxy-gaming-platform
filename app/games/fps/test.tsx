'use client'

export default function TestFPSPage() {
  return (
    <div style={{ padding: '20px', background: '#000', color: '#fff', minHeight: '100vh' }}>
      <h1>FPS Test Page</h1>
      <p>This is a simple test to check if the basic FPS route works.</p>
    </div>
  )
}