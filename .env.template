# ===============================================================================
# GLXY Gaming Platform - Environment Variables Template
# ===============================================================================
# ANLEITUNG:
# 1. Kopiere diese Datei zu ".env.local" für Development
# 2. Ersetze ALLE Platzhalter mit echten Werten
# 3. NIEMALS .env oder .env.local in Git committen!
# ===============================================================================

# ===== CORE SETTINGS =====
NODE_ENV=development
DOMAIN=localhost

# ===== NEXTAUTH AUTHENTICATION =====
# Generiere mit: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET=<GENERATE_WITH_CRYPTO_RANDOMBYTES_32_BASE64>
NEXTAUTH_URL=http://localhost:3000
JWT_SECRET=<GENERATE_WITH_CRYPTO_RANDOMBYTES_32_BASE64>

# ===== DATABASE (PostgreSQL) =====
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://glxy_user:password@localhost:5432/glxy_gaming?schema=public
POSTGRES_USER=glxy_user
POSTGRES_PASSWORD=<SET_SECURE_PASSWORD>
POSTGRES_DB=glxy_gaming

# ===== REDIS CACHE =====
REDIS_URL=redis://default:password@localhost:6379
REDIS_PASSWORD=<SET_SECURE_PASSWORD>
REDIS_HOST=localhost
REDIS_PORT=6379

# ===== GOOGLE OAUTH (Optional) =====
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=<YOUR_GOOGLE_CLIENT_ID>
GOOGLE_CLIENT_SECRET=<YOUR_GOOGLE_CLIENT_SECRET>

# ===== GITHUB OAUTH (Optional) =====
# Get from: https://github.com/settings/developers
GITHUB_CLIENT_ID=<YOUR_GITHUB_CLIENT_ID>
GITHUB_CLIENT_SECRET=<YOUR_GITHUB_CLIENT_SECRET>

# ===== SOCKET.IO (Real-time Gaming) =====
SOCKET_IO_SECRET=<GENERATE_WITH_CRYPTO_RANDOMBYTES_32_BASE64>
SOCKET_IO_PORT=3001
SOCKET_IO_CORS_ORIGIN=http://localhost:3000,http://localhost

# ===== ENCRYPTION KEYS (Optional but recommended) =====
API_ENCRYPTION_KEY=<GENERATE_WITH_CRYPTO_RANDOMBYTES_32_BASE64>
SESSION_ENCRYPTION_KEY=<GENERATE_WITH_CRYPTO_RANDOMBYTES_32_BASE64>
CSRF_SECRET=<GENERATE_WITH_CRYPTO_RANDOMBYTES_32_BASE64>
COOKIE_SECRET=<GENERATE_WITH_CRYPTO_RANDOMBYTES_32_BASE64>

# ===== SMTP/EMAIL (Optional - für E-Mail-Verifikation) =====
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=<YOUR_EMAIL>
SMTP_PASS=<YOUR_EMAIL_APP_PASSWORD>

# ===== SENTRY ERROR TRACKING (Optional) =====
SENTRY_DSN=<YOUR_SENTRY_DSN>
NEXT_PUBLIC_SENTRY_DSN=<YOUR_SENTRY_DSN>
SENTRY_ORG=glxy-project
SENTRY_PROJECT=glxy-gaming
SENTRY_ENVIRONMENT=development
DISABLE_SENTRY=true

# ===== ADMIN CONFIGURATION =====
# Komma-separierte Liste von Admin-E-Mails
ADMIN_EMAILS=admin@example.com
ADMIN_USER_IDS=

# ===== PUBLIC ENVIRONMENT VARIABLES =====
# Diese werden im Frontend Bundle inkludiert
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_WS_URL=ws://localhost:3001
NEXT_PUBLIC_SOCKET_PATH=/api/socket/io

# ===============================================================================
# SECURITY NOTES:
# - Generiere alle Secrets mit: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# - Verwende niemals die gleichen Secrets wie in diesem Template
# - Halte alle Secrets geheim und teile sie NIEMALS öffentlich
# - .env.local ist in .gitignore und wird NICHT committed
# ===============================================================================
